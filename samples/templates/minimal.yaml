virtualHosts:
  - name: sales
    exchanges:
      - name: orders
        type: topic
    queues:
      - name: orders.created
        type: classic
    bindings:
      - sourceExchange: orders
        destination: orders.created
        destinationType: queue
        routingKey: orders.created
